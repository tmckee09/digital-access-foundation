# Copy to .env.local and fill in values as needed.
# Only use NEXT_PUBLIC_ for values that are safe to expose in the browser.

# Example placeholders (replace or remove as needed)
NEXT_PUBLIC_SITE_URL=
CONTACT_EMAIL=
